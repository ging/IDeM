<div id="followers" class="tab-pane in active">
  <div class="row">
    <ul>
      <% profileLoopData["followers"].each do |loop_contact| %>
        <li><%= render 'contacts/loop_contact', :contact => loop_contact, :showActions => isProfileOwner %></li>
      <% end %>
    </ul>
  </div>
</div>