<div class="row">
	<div class="col-md-8">
		<h4><%= publication.title %></h4>
		<h5><%= publication.authors_name %></h5>
		<h6><%= publication.publication_name %>, <%= publication.publication_date %></h6>
	</div>
	<% if showActions %>
	<div class="col-md-4 publication_actions">
		<div class="button-secondary"><%= link_to('Create presentation', new_presentation_path({:publication => publication.id})) %></div>
		<div class="button-secondary"><%= link_to('Schedule webinar', new_webinar_path({:publication => publication.id})) %></div>
	</div>
	<% end %>
</div>