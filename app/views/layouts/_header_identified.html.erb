<div id="idem-logo"><%= link_to(image_tag('idem_logo.svg', :alt => 'idem'), '/', :class => "logo"); %></div>
<div class="button-secondary" id="login"><%= link_to('Logout', destroy_user_session_path, :method => :delete) %></div>
<div id="profile">
	<%= image_tag(@user.avatar_url, :alt => @user.name) %>
	<%= link_to(current_user.name, user_path(current_user)) %>
</div>
